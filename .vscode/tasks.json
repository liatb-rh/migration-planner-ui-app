{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Deploy DB",
      "type": "shell",
      "command": "make",
      "args": ["-C", "${workspaceFolder}/../migration-planner", "deploy-db"],
      "presentation": {
        "close": true
      }
    },
    {
      "label": "Wait for Backend API to be ready",
      "type": "shell",
      "command": "while ! curl -s http://0.0.0.0:3443/ > /dev/null; do sleep 1; done",
      "presentation": {
        "close": true
      }
    }
  ]
}
